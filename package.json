{
  "name": "bineisa-stocks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "test": "node --test",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "redis": "6.2.1"
  }
}
